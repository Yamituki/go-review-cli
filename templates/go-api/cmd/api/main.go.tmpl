package main

import (
	"log"

	"{{.ModuleName}}/internal/router"
)

func main() {
	r := router.NewRouter()

	log.Println("Starting server on :8080")
	if err := router.Start(r, ":8080"); err != nil {
		log.Fatal("Failed to start server:", err)
	}
}